# SIAK App - CodeIgniter 4

Sistem Informasi Akademik sederhana menggunakan CodeIgniter 4 + SB Admin 2.

## Cara Install
1. Clone repo: `git clone ...`
2. `composer install`
3. Copy `.env.example` → `.env`, sesuaikan konfigurasi database
4. Import SQL dari folder `database/`
5. `php spark serve`